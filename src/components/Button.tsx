import { Payload } from 'payload'
import React from 'react'
import { ButtonClient } from './ButtonClient.js'

export const Button = ({ payload }: { payload: Payload }) => {
  return <ButtonClient />
}

export default Button
